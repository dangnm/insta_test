.photo-grid{:'data-match-columns' => 'true'}
  - photos.each do |p|
    .photo-item.col-xs-6.col-ms-6.col-sm-4.col-md-4.col-lg-4{:class => "item-#{p.id}" } 
      .caption=p.caption
      .photo
        %img{:src => p.data.url, onerror: "this.src='/image-not-found.jpg'"}

- content_for :js do
  :javascript
    var matchColumns = new CustomLib.responsive.MatchColumns($(".photo-grid"));
    matchColumns.update();