- @photos.each do |p|
  .photo-item{:class => "item-#{p.id}" } 
    .caption=p.caption
    .photo
      %img{:src => p.data.url}
    .comment-section
      = render('comments/comment_section', :photo => p)